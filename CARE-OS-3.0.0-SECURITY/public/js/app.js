const frame=document.getElementById("moduleFrame"),home=document.getElementById("hubHome"),moduleView=document.getElementById("moduleView"),moreView=document.getElementById("moreView");
function navButton(name){return document.querySelector(`.dock [data-hub="${name}"]`)}
function setActive(button){document.querySelectorAll(".dock button").forEach(x=>x.classList.remove("active"));button?.classList.add("active")}
function openHub(name,url,button){setActive(button||navButton(name));moreView.classList.add("hidden");if(name==="dashboard"){moduleView.classList.add("hidden");home.classList.remove("hidden");frame.src="";history.replaceState(null,"","/app");return}home.classList.add("hidden");moduleView.classList.remove("hidden");if(frame.getAttribute("src")!==url)frame.src=url;history.replaceState(null,"",`/app#${name}`)}
function openMore(button){setActive(button);home.classList.add("hidden");moduleView.classList.add("hidden");moreView.classList.remove("hidden");history.replaceState(null,"","/app#more")}
async function changeUser(){try{await fetch("/api/auth/logout",{method:"POST",credentials:"same-origin"})}catch{}localStorage.clear();window.location.replace("/launch")}
function boot(){const user=window.OS?.user;document.getElementById("userLabel").textContent=user?.name?`${user.name} · ${user.role||"C.A.R.E"}`:"Operations workspace";const route=location.hash.slice(1),map={operations:"/operations-center.html",communications:"/communications-center.html",performance:"/performance-center.html",rooms:"/rooms-manager",payroll:"/payroll.html",employees:"/employee-center"};if(route==="more")openMore(navButton("more"));else if(map[route])openHub(route,map[route],navButton(route)||navButton("more"))}
window.addEventListener("message",event=>{if(event.data?.type==="care-open-module")openHub(event.data.name,event.data.url,navButton(event.data.nav||"more"))});document.addEventListener("DOMContentLoaded",boot);if(typeof io==="function"){const socket=window.OS?.socket||io();socket.on("connect",()=>document.getElementById("liveText").textContent="En vivo");socket.on("disconnect",()=>document.getElementById("liveText").textContent="Reconectando")}
